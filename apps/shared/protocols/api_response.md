# 统一 API 响应协议

## 1. 标准结构

所有管理面接口统一返回以下结构：

1. `code`：平台统一错误码，成功固定为 `0`
2. `message`：中文错误消息或成功描述
3. `request_id`：请求追踪标识
4. `data`：实际业务数据对象

## 2. 约束

1. HTTP 状态码与 `code` 语义保持一致
2. `message` 必须使用中文
3. 错误时 `data` 仍返回对象，禁止返回裸字符串
4. 审计日志、测试报告、构建结果需保留 `request_id` 追踪位扩展能力
