# ai-test 开发计划

## 1. 模块目标

落地模型测试、批量测试、测试报告与导出能力。

## 2. 工作分解

| 编号 | 工作项 | 状态 |
| --- | --- | --- |
| TT1 | 搭建 ai-test 前后端工程骨架 | 已完成 |
| TT2 | 对接 ai-train 模型查询接口 | 已完成 |
| TT3 | 实现单接口测试流程 | 已完成 |
| TT4 | 实现批量测试流程 | 已完成 |
| TT5 | 实现 GPU/CPU 自动切换推理测试 | 已完成 |
| TT6 | 实现测试报告生成与导出 | 已完成 |
| TT7 | 实现测试镜像 | 已完成 |
| TT8 | 联调与验收 | 已完成 |

## 3. 进度维护要求

每次开发前后更新状态、风险、阶段小结。

## 4. 当前进度更新

### 4.1 本轮已完成

1. 已确认 ai-test 为 ai-train 后的下一个实施模块。
2. 已完成 ai-test 模块设计、开发计划与工程规范核对。
3. 已建立 ai-test 后端 FastAPI + SQLite 工程骨架与基础配置。
4. 已建立 ai-test 前端 React + TypeScript + Vite 工程骨架。
5. 已实现 ai-train 模型目录同步、快照回退与查询接口。
6. 已实现单接口测试、批量测试、GPU/CPU 回退与超时控制基础链路。
7. 已实现测试结果持久化、HTML/JSON/PDF 报告生成与导出能力。
8. 已接入 ai-test 前端真实查询页面，展示模型、测试任务与测试报告列表。
9. 已新增 ai-test CUDA 11.8 Dockerfile 与运行说明。
10. 已完成 ai-test 后端测试、前端 build/lint 与 Docker 构建阶段校验。

### 4.2 本轮进行中

1. ai-test 模块当前开发计划工作项已完成，后续进入增强迭代阶段。
2. 后续增强方向包括真实 ONNX 模型推理接管、测试样本上传与更完整的报告模板。

### 4.3 阶段小结

ai-test 模块已完成本轮计划内的工程骨架、模型目录同步、单测与批测、GPU/CPU 回退、测试报告生成导出、前端管理台展示与 CUDA 11.8 镜像文件落地，形成可运行、可校验、可持续迭代的基础版本闭环。
