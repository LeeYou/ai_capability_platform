# ai-test 开发计划

## 1. 模块目标

持续完善内部研发验收与交付验收能力，补齐生产镜像验收、C++ HTTP 主服务回归测试与更专业的测试报告能力。

## 2. 工作分解

| 编号 | 工作项 | 状态 |
| --- | --- | --- |
| TT1 | 搭建 ai-test 前后端工程骨架 | 已完成 |
| TT2 | 对接 ai-train 模型查询接口 | 已完成 |
| TT3 | 实现单接口测试流程 | 已完成 |
| TT4 | 实现批量测试流程 | 已完成 |
| TT5 | 实现 GPU/CPU 自动切换推理测试 | 已完成 |
| TT6 | 实现测试报告生成与导出 | 已完成 |
| TT7 | 实现测试镜像 | 已完成 |
| TT8 | 首期联调与验收 | 已完成 |
| TT9 | 生产镜像验收任务与回归脚本体系 | 已完成 |
| TT10 | 面向 C++ HTTP 主服务的性能/稳定性验收 | 已完成 |
| TT11 | 研发验收与交付验收双视角报告模板 | 已完成 |
| TT12 | 测试样本与训练标注 schema 映射收敛 | 已完成 |
| TT13 | 模型 / 插件 / license / revision 统一验收证据链 | 已完成 |
| TT14 | 新增能力模板化回归与交付验收用例 | 已完成 |
| TT15 | 单测工作台向导化与在线结果对比重构 | 已完成 |
| TT16 | 批量测试任务创建、执行、问题样本分析工作台 | 已完成 |
| TT17 | 生产验收工作台与 acceptance / pressure 日志视图 | 已完成 |
| TT18 | 报告中心、证据链与“推送授权 / 构建”主流程收口 | 已完成 |
| TT19 | ai-test 首页概览、待测清单与推荐动作重构 | 已完成 |
| TT20 | 测试任务 `execution_mode` 显式化与真实 / 仿真执行路径区分 | 已完成 |
| TT21 | 测试服务事务边界收敛、结果持久化重构与 shared contract validator 接入 | 已完成 |

## 3. 进度维护要求

每次开发前后更新状态、风险、阶段小结，并同步对照 `docs/05_评审/代码逻辑自洽整改清单.md` 中 RTS-01 ~ RTS-05。

## 4. 当前进度更新

### 4.1 已完成

1. 已完成 ai-test 基础版工程骨架、模型目录同步、单测/批测、GPU/CPU 回退与测试报告导出能力。
2. 已完成基础前端管理台与 CUDA 11.8 测试镜像落地。
3. 已完成基础测试回归与 Docker 构建校验。

### 4.2 进行中

1. 当前已完成 TT15-TT19：前端已从展示型列表页升级为“中枢视图 / 单测工作台 / 批量测试 / 生产验收 / 报告中心”五段式工作台。
2. 当前已完成 TT15：单测页面已改造为向导式输入与结果对比布局，可直接从待测模型进入单测、查看用例级期望 / 实际结果与失败信息。
3. 当前已完成 TT16：批量测试页面已补齐任务创建、用例清单输入、失败样本回看与基线模板维护入口。
4. 当前已完成 TT17：生产验收页面已补齐镜像 / 目标地址 / 推理载荷配置与脚本结果视图，统一承接 acceptance / pressure 结果展示。
5. 当前已完成 TT18：报告中心已补齐研发 / 交付双视角切换、摘要证据链展示与导出 / 下游推进入口。
6. 当前已完成 TT19：首页已重构为待测模型、失败任务与推荐动作驱动的测试中枢视图。
7. 当前已完成 TT20：单测 / 批测 / 报告中心已统一输出 `execution_mode`、`execution_risk` 与 `evidence_chain.execution`，真实测试适配器与仿真回退路径已在 API、报告与前端工作台显式区分。
8. 当前已完成 TT21：`test_service.py` 已收敛为编排层，新增 `test_execution_service.py` 承接执行模式判定与单用例执行；`create_test_task()` 已从多次 `commit` 收敛为 `flush + 单次 commit` 主事务，`report_service.py` 已接入 shared `validate_test_report_summary()`。

### 4.3 未完成

1. 本轮模块级整改已完成，下一阶段不再继续停留在 ai-test，本模块后续仅在跨模块 shared contract 扩展时跟进补充更多报告 / 交付契约校验。

### 4.4 阶段小结

ai-test 已完成 TT20-TT21，当前模块计划已校准到 R16：在上一轮测试工作台重构基础上，本轮已完成测试后端的结构性收敛，补齐了 `execution_mode` 与风险提示的显式语义，区分了真实 / 仿真执行路径，将测试报告摘要纳入 shared contract validator，并收敛了单测 / 批测主链路的事务边界。至此 ai-test 已形成与 ai-train 一致的模块级收口样板，下一步将按既定顺序推进 ai-license-mgr。同时本轮已开始推进 RC-03：已在 `frontend-common` 新增统一工作台壳层组件（`WorkspaceShell`），并在 ai-test 前端完成接入，以减少跨模块重复壳层代码并为后续抽象公共 hooks / 状状态组件打底。
