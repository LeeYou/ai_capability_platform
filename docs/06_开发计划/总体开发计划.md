# 总体开发计划

## 1. 计划目标

按照“上层平台完整、运行底座扎实”的目标分阶段推进平台建设：保留当前平台在内部研发测试流程上的优势，重点吸收 `ai_platform` 在 Runtime 内核、交付目录与验收体系、生产侧 License / 热更新 / 回滚机制方面的成熟设计。

## 2. 计划状态说明

- 状态值：未开始 / 进行中 / 已完成 / 阻塞
- 当前版本状态：已完成（R2/R3/R4/R5/R6/R7 已完成，当前阶段代码实现、模块计划与总体计划已完成统一对齐）

## 3. 总体阶段划分

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| P0 | 文档体系建设与首轮评审 | 已完成 |
| P1 | 工程骨架与基础规范落地 | 已完成 |
| P2 | ai-train 基础版实现 | 已完成 |
| P3 | ai-test 基础版实现 | 已完成 |
| P4 | ai-license-mgr 基础版实现 | 已完成 |
| P5 | ai-builder 基础版实现 | 已完成 |
| P6 | ai-prod 基础版实现 | 已完成 |
| P7 | 单机交付 SDK 基础版实现 | 已完成 |
| P8 | 首期联调、验收与交付材料完善 | 已完成 |
| R1 | 基于 ai_platform 的设计文档与开发计划刷新 | 已完成 |
| R2 | ai-prod 运行底座融合实现（C++ HTTP + C++ Runtime） | 已完成 |
| R3 | ai-builder 标准交付包与验收体系融合实现 | 已完成 |
| R4 | ai-license-mgr / ai-prod License、热更新、回滚收敛 | 已完成 |
| R5 | SDK / delivery_package / tools / docs 统一收敛 | 已完成 |
| R6 | ai-test 生产镜像验收体系与回归链路增强 | 已完成 |
| R7 | 内部管理 UI 专业化增强与总体联调复审 | 已完成 |

## 4. 跨模块公共工作项

1. 建立 mono-repo 目录结构
2. 建立前后端基础工程
3. 建立共享元数据协议
4. 建立错误码、manifest、license schema
5. 建立 Docker 基础镜像与挂载目录模板
6. 建立 CI、质量检查与文档更新流程
7. 建立标准 `delivery_package/` 目录规范
8. 建立生产运行底座与内部测试外壳的分层边界

## 5. 进度跟踪要求

1. 每个模块实现前先核对设计文档与模块计划。
2. 实现前更新模块计划实际状态。
3. 实现完成后更新模块计划、总体计划与阶段小结。
4. 提交代码时使用中文提交说明。
5. 每轮实现完成后必须比对设计文档、开发计划与实际代码，确认未跑偏。

## 6. 当前里程碑结论

1. 基础版六个业务模块、单机交付 SDK、共享 schema、Docker 基础镜像、compose 编排与 CI 已完成。
2. 已完成与 `ai_platform` 的深度对比评估，并明确后续目标。
3. 当前已完成设计文档与开发计划的融合刷新，下一阶段进入运行底座与交付体系实现阶段。

## 7. 当前阶段进度更新

### 7.1 已完成

1. 已完成 P0-P8 首期基础版闭环实现。
2. 已完成 `ai_platform` 对比评估与评估报告输出。
3. 已完成产品设计、总体架构、技术选型、模块设计、总体开发计划与各模块开发计划刷新。
4. 已明确“内部研发平台 + 外部运行底座”的组合产品目标。
5. 已明确 ai-prod 下一阶段采用 C++ HTTP + C++ Runtime + 插件模式作为生产主链路。
6. 已明确 ai-builder 下一阶段输出标准 `delivery_package/`、工具、文档与验收材料。
7. 已明确 ai-license-mgr、ai-prod、SDK 在 License / 热更新 / 回滚 / 交付目录上的收敛方向。
8. 已完成 ai-license-mgr 与 ai-prod 运行态版本约束规则的一轮收敛，为后续 `license_tool` 与交付材料统一奠定基础。
9. 已完成 `license_tool` 默认发布归档、README/错误码/硬件指纹说明与 delivery_package/tools 标准物料收口。
10. 已完成 ai-license-mgr 私钥轮转、隔离、策略迁移与非激活密钥阻断增强。
11. 已完成 SDK 标准目录、license_tool/文档/示例工程收敛，以及 package 级验收清单与快速校验脚本增强。
12. 已完成 ai-prod 在 GPU 插件装载/生命周期失败场景下的 CPU 自动回退编排与观测增强。
13. 已完成 ai-prod 请求全生命周期耗时观测与能力级时延聚合增强。
14. 已完成 ai-prod 请求级 SLA 截止时间控制与 deadline 违约观测增强。
15. 已完成 ai-prod 结构化审计日志与请求关联字段统一收口增强。
16. 已完成 ai-prod capability 级短时批次聚合与批次调度观测增强。
17. 已完成 ai-prod capability 级运行时编排元数据与全局资源编排诊断增强。
18. 已完成 ai-train 标注台/训练台交互增强、模型包标准 manifest 与交付元数据收敛。
19. 已完成 ai-license-mgr、ai-builder、ai-prod 三个内部管理台的本轮专业化工作台增强。
20. 已完成五个业务模块前端的跨模块联调导航与统一联调工作区块增强。
21. 已完成五个业务模块前端共享 R7 workspace 配置、公共样式抽象与总体联调复审展示收口。

### 7.2 已完成

1. 融合增强版整体计划已完成当前轮目标收口：R2/R3/R4/R5/R6/R7 均已完成，各模块计划与总体计划已同步回溯核对实际进度。
2. ai-prod 首轮已完成 C++ HTTP 服务骨架、接口代理、编译验证与 smoke 校验，为后续 Runtime 替换提供了过渡主链路。
3. ai-prod 随后已完成 C++ 直读 snapshot、catalog/pool、reload/rollback、真实插件执行、启动 bootstrap、状态机、请求级跟踪、payload codec、revision 级回滚恢复、统一指标聚合等主链路收口。
4. ai-prod 当前在已完成 capability 级 `queue_wait_timeout_ms` / `max_pending_request_count` 调度策略元数据透传、GPU 生命周期失败场景下的 CPU 自动回退编排、请求全生命周期耗时观测与能力级时延聚合、请求级 SLA 截止时间控制与 deadline 违约观测、结构化审计日志与请求关联字段统一收口、capability 级短时批次聚合与批次调度观测增强的基础上，进一步补齐 capability 级运行时编排元数据透传与 `ResourceOrchestrator` 全局资源编排诊断，R2 已正式完成。
5. ai-builder 已完成 B9/B10/B11，对应 R3“标准交付包与验收体系融合实现”已在代码层面收口。
6. SDK / delivery_package / tools / docs 的统一收敛已完成最终收口：在 ai-builder 侧 delivery_package、tools、docs、mount_template 与摘要产物基础之上，本轮继续补齐 shared schema、example、SDK package 级验收清单/版本清单/交付摘要对齐与 checksum 字段收敛，R5 已正式完成。
7. ai-prod 当前已完成 P9-P34：生产镜像/compose 已切换为“C++ HTTP 对外主入口 + Python backend 仅容器内壳层”的双进程交付主链路，infer 热路径、启动 bootstrap、reload/rollback 管理与 runtime 诊断均已由 C++ 主链路收口，模块开发计划已全部完成。
8. ai-test 当前已完成 TT9/TT10/TT11：已新增生产镜像验收任务模型、验收任务接口、性能/稳定性验收基线模型与基线查询/写入接口，并对 ai-prod `acceptance_check.py` / `pressure_smoke.py` 形成统一编排与结果对比输出，同时补齐研发验收 / 交付验收双视角报告模板，R6 已完成。
9. ai-license-mgr 当前已完成 L8/L9/L10：ai-prod C++/Python 运行态已与 ai-license-mgr 签发侧对齐版本约束规则，统一支持 `allowed_versions`、`prefix`、`min_version`、`max_version` 与缺失产品版本拒绝语义；同时已补齐 `license_tool_release` 发布归档、默认 `license_tool` source bundle 同步/查询/导出，以及密钥轮转版本、前序密钥链路、状态变更元数据、策略迁移与非激活密钥阻断逻辑，R4 在授权签发侧已完成收口。
12. 单机交付 SDK 当前已完成 S7/S8/S9：已按 `sdk_*` 标准目录统一 Linux/JNI/Windows SDK 输出，并补齐 `tools/license_tool`、部署/验收文档、快速校验脚本与 package 级 `acceptance_checklist.json`，SDK 模块开发计划已全部完成。
13. ai-train 当前已完成 T9/T10/T11：已补齐样本级标注编辑、批量提交、训练日志快照、执行计划与训练结果回显，并统一标准化模型 manifest、校验文件与面向 ai-test / ai-builder 的交付元数据。
14. R7 当前已完成第一轮内部管理 UI 专业化增强：ai-license-mgr 已补齐客户/密钥/策略/签发/工具发布工作台，ai-builder 已补齐构建任务创建、任务详情与 delivery_package 摘要视图，ai-prod 已补齐能力目录、在线控制台与 revision/operation 工作台。
15. R7 当前已完成第二轮跨模块统一设计增强与最终收口：五个业务模块前端已统一补齐跨模块联调导航入口、当前模块标识、联调复审清单、共享 workspace 配置、公共样式抽象与总体联调复审展示，R7 已正式完成。

### 7.3 未完成

1. 暂无；当前总体开发计划内各阶段已完成，后续若继续增强，将进入新一轮增量规划。
### 7.4 阶段小结

当前平台已具备内部研发测试流程的完整闭环能力。随着 ai-prod 完成 P9-P34、ai-train 完成 T9/T10/T11、ai-builder 完成 B9/B10/B11、ai-test 完成 TT9/TT10/TT11、ai-license-mgr 完成 L8/L9/L10、单机交付 SDK 完成 S7/S8/S9，平台一侧已经具备交付主入口、能力目录管理、轻量实例池状态维护、交付级验收脚本、标准 `delivery_package/` 目录输出、ai-prod 生产镜像构建上下文 tarball、挂载模板、验收/运维工具、部署文档，以及验收清单、版本清单、交付摘要等最终收口物料；同时 ai-prod 的 infer 热路径已在 snapshot 可用时由 C++ 直接完成请求解析、license quick check、设备选择、实例池借还、真实插件动态加载执行、结果生成与基础日志审计，启动阶段的 bootstrap 首次装载与 reload/rollback 管理链路也已由 C++ 直接完成资源扫描、revision/operation SQLite 持久化、runtime snapshot 初始/重写与 catalog/pool 刷新，并进一步补齐 runtime 显式状态机、请求级 in-flight 跟踪、RAII 请求租约、能力级执行指标聚合、插件 `warmup` / `health_check` 生命周期钩子、统一输入 `payload codec`、切换互斥保护、基于 revision 的完整能力资源快照持久化与真实回滚恢复，以及 capability 级 `max_batch_size` / `instance_count` 元数据透传、繁忙拒绝观测、基础请求排队等待与调度观测、capability 级排队策略元数据透传、GPU 插件装载/生命周期失败场景下的 CPU 自动回退编排与观测、请求全生命周期耗时观测与能力级时延聚合、请求级 SLA 截止时间控制与 deadline 违约观测、结构化审计日志与请求关联字段统一收口、capability 级短时批次聚合、`batch_wait_timeout_ms` 配置透传与 formed/full/timeout batch 观测，以及 capability 级运行时编排元数据透传与 `ResourceOrchestrator` 全局资源编排诊断面；ai-license-mgr 与 ai-prod 运行时的版本约束语义、`license_tool` 发布归档、密钥轮转/隔离、热更新与回滚链路也已完成跨模块收敛，R4 正式完成；SDK 模块则已按 `sdk_*` 标准目录统一 Linux/JNI/Windows 交付形态，并补齐 `tools/license_tool`、部署/验收文档、快速校验脚本与 package 级验收清单，同时与 ai-builder 一起完成 shared schema、example、验收清单、版本清单、交付摘要、mount_template、tools/docs 清单与 checksum 字段契约的统一收敛，R5 已正式完成。本轮在此基础上完成 R7 的内部管理 UI 专业化增强与五模块跨模块联调导航/共享 workspace 收口，至此当前总体开发计划内定义的增强阶段均已完成。后续若继续推进，将以当前平台为主平台、以 `ai_platform` 为运行内核与交付规范参考实现，进入新一轮增量规划。
