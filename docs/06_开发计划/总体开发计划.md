# 总体开发计划

## 1. 计划目标

按照"上层平台完整、运行底座扎实"的目标分阶段推进平台建设：保留当前平台在内部研发测试流程上的优势，持续完善 Runtime 内核、交付目录与验收体系、生产侧 License / 热更新 / 回滚机制。

## 2. 计划状态说明

- 状态值：未开始 / 进行中 / 已完成 / 阻塞
- 当前版本状态：进行中（R8-R15 已完成；当前已进入“平台公共底座与服务分层重构”的新一轮阶段）

## 3. 总体阶段划分

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| P0 | 文档体系建设与首轮评审 | 已完成 |
| P1 | 工程骨架与基础规范落地 | 已完成 |
| P2 | ai-train 基础版实现 | 已完成 |
| P3 | ai-test 基础版实现 | 已完成 |
| P4 | ai-license-mgr 基础版实现 | 已完成 |
| P5 | ai-builder 基础版实现 | 已完成 |
| P6 | ai-prod 基础版实现 | 已完成 |
| P7 | 单机交付 SDK 基础版实现 | 已完成 |
| P8 | 首期联调、验收与交付材料完善 | 已完成 |
| R1 | 基础架构评估、设计文档与开发计划刷新 | 已完成 |
| R2 | ai-prod 运行底座融合实现（C++ HTTP + C++ Runtime） | 已完成 |
| R3 | ai-builder 标准交付包与验收体系融合实现 | 已完成 |
| R4 | ai-license-mgr / ai-prod License、热更新、回滚收敛 | 已完成 |
| R5 | SDK / delivery_package / tools / docs 统一收敛 | 已完成 |
| R6 | ai-test 生产镜像验收体系与回归链路增强 | 已完成 |
| R7 | 内部管理 UI 专业化增强与总体联调复审 | 已完成 |
| R8 | 代码逻辑自洽整改清单与文档基线刷新 | 已完成 |
| R9 | ai-train 标注 / 训练 / 模型包契约整改 | 已完成 |
| R10 | ai-license-mgr / ai-prod / ai-sdk 授权核心一致性整改 | 已完成 |
| R11 | ai-builder / ai-prod 真实插件交付链整改 | 已完成 |
| R12 | ai-test 端到端验收证据链整改 | 已完成 |
| R13 | 新增能力模板化接入与全链路回归复审 | 已完成 |
| R14 | 授权平台字段（OS/最低系统版本/架构/应用名）增量整改 | 已完成 |
| R15 | Web 产品化体验、模块工作台重构与统一门户规划 | 已完成 |
| R16 | 平台公共底座、共享契约执行层与按模块服务分层重构 | 进行中 |

## 4. 跨模块公共工作项

1. 建立 mono-repo 目录结构
2. 建立前后端基础工程
3. 建立共享元数据协议
4. 建立错误码、manifest、license schema
5. 建立 Docker 基础镜像与挂载目录模板
6. 建立 CI、质量检查与文档更新流程
7. 建立标准 `delivery_package/` 目录规范
8. 建立生产运行底座与内部测试外壳的分层边界

## 5. 进度跟踪要求

1. 每个模块实现前先核对设计文档与模块计划。
2. 实现前更新模块计划实际状态。
3. 实现完成后更新模块计划、总体计划与阶段小结。
4. 提交代码时使用中文提交说明。
5. 每轮实现完成后必须比对设计文档、开发计划与实际代码，确认未跑偏。

## 6. 当前里程碑结论

1. 基础版六个业务模块、单机交付 SDK、共享 schema、Docker 基础镜像、compose 编排与 CI 已完成。
2. 已完成平台架构深度评估，并明确后续目标。
3. 上一轮增强版计划已完成收口，当前进入“平台公共底座收敛 + 共享契约执行层落地 + 按模块服务分层重构”的新一轮阶段。

## 7. 当前阶段进度更新

### 7.1 已完成

1. 已完成 P0-P8 首期基础版闭环实现。
2. 已完成平台架构评估与评估报告输出。
3. 已完成产品设计、总体架构、技术选型、模块设计、总体开发计划与各模块开发计划刷新。
4. 已明确“内部研发平台 + 外部运行底座”的组合产品目标。
5. 已明确 ai-prod 下一阶段采用 C++ HTTP + C++ Runtime + 插件模式作为生产主链路。
6. 已明确 ai-builder 下一阶段输出标准 `delivery_package/`、工具、文档与验收材料。
7. 已明确 ai-license-mgr、ai-prod、SDK 在 License / 热更新 / 回滚 / 交付目录上的收敛方向。
8. 已完成 ai-license-mgr 与 ai-prod 运行态版本约束规则的一轮收敛，为后续 `license_tool` 与交付材料统一奠定基础。
9. 已完成 `license_tool` 默认发布归档、README/错误码/硬件指纹说明与 delivery_package/tools 标准物料收口。
10. 已完成 ai-license-mgr 私钥轮转、隔离、策略迁移与非激活密钥阻断增强。
11. 已完成 SDK 标准目录、license_tool/文档/示例工程收敛，以及 package 级验收清单与快速校验脚本增强。
12. 已完成 ai-prod 在 GPU 插件装载/生命周期失败场景下的 CPU 自动回退编排与观测增强。
13. 已完成 ai-prod 请求全生命周期耗时观测与能力级时延聚合增强。
14. 已完成 ai-prod 请求级 SLA 截止时间控制与 deadline 违约观测增强。
15. 已完成 ai-prod 结构化审计日志与请求关联字段统一收口增强。
16. 已完成 ai-prod capability 级短时批次聚合与批次调度观测增强。
17. 已完成 ai-prod capability 级运行时编排元数据与全局资源编排诊断增强。
18. 已完成 ai-train 标注台/训练台交互增强、模型包标准 manifest 与交付元数据收敛。
19. 已完成 ai-license-mgr、ai-builder、ai-prod 三个内部管理台的本轮专业化工作台增强。
20. 已完成五个业务模块前端的跨模块联调导航与统一联调工作区块增强。
21. 已完成五个业务模块前端共享 R7 workspace 配置、公共样式抽象与总体联调复审展示收口。
22. 已完成代码逻辑自洽审查结论整理，并新增整改清单文档、总体架构整改基线、模块设计整改映射与新一轮总体/模块开发计划基线。
23. 已完成 ai-train 模块本轮整改：补齐任务类型化标注 schema、训练输入适配、训练执行与导出链路、模型 runtime 契约与统一模板脚手架，R9 已完成。
24. 已完成 ai-license-mgr 模块本轮整改：补齐授权金标准测试向量、稳定诊断字段、tool bundle 契约与 ai-prod Python 校验一致性回归基线。
25. 已完成 ai-prod / ai-sdk 在 R10 中的第一阶段整改：ai-prod Python / C++ runtime 已补齐稳定授权诊断字段，ai-sdk 已补齐 license_tool 诊断契约与测试向量物料。
26. 已完成 ai-prod 在 R10 中的第二阶段整改：C++ runtime resource scanner 与 Python 验收外壳已补齐 ai-train 模型包 manifest、ai-builder 插件 manifest 的强校验与强消费收口，无效资源会在运行时切换阶段被跳过并记录失败明细。
27. 已完成 ai-prod 在 R10 中的第三阶段整改：C++ runtime 已补齐 capability 级 `admission_checklist` 与插件预装载门禁，bootstrap / reload / rollback 会拦截未通过 ABI / 运行时装载探测的能力，并在 `source_summary.admission_gate_failures` 中记录失败原因。
28. 已完成 ai-prod 在 R10 中的第四阶段整改：公开交付验收脚本与回归测试已补齐 `/api/v1/*` 主链路的 bootstrap / license-reload / reload / infer / rollback 端到端校验，验收结果会直接验证 revision 切换与切换后的再次推理。
29. 已完成 ai-sdk 在 R10 中的 S11-S12 整改：SDK 包生成已强依赖 ai-builder 真实交付目录，manifest 附带 `source_builder_manifest`，并内置 `validation/verify_sdk_package.py` 校验脚本、示例工程与端到端回归测试，R10 全部完成，G3 整改分组收口。
30. 已完成 R14：ai-license-mgr / ai-prod / ai-sdk 已统一新增 `operating_system`、`min_operating_system_version`、`system_architecture`、`application_name` 授权平台字段，Python / C++ runtime 与 `license_tool` / SDK 交付物均已同步平台准入诊断契约。
31. 已完成 R11（B12/B13/B14）：ai-builder 插件构建链从 echo 占位桩切换为 ONNX Runtime 任务类型感知 C++ 模板；模型包文件随 SDK 一起交付；建立 source_train_task_id → model_manifest → builder_task → delivery_package 全链路追溯；交付前新增运行时可装载性与一致性校验，输出 pre_delivery_validation.json；RB-01/RB-02/RB-03 整改收口。

32. 已完成 R12（TT12/TT13/TT14）：ai-test 补齐任务类型感知仿真推理、全链路验收证据链（evidence_chain）与模板化回归任务接口，TT1–TT14 全部完成，RTS-01/02/03 整改收口。
33. 已完成 R13：跨模块新增能力模板化接入脚手架（onboard_capability.py）与接入指南（docs/07_新增能力接入指南.md）落地，支持 4 种任务类型全链路 12 步接入清单，G6 整改收口。
34. 已完成 Web 入口层打通：ai-train、ai-test、ai-license-mgr、ai-builder 已可通过模块端口直接打开页面，并已补齐访问说明文档。

### 7.2 进行中

1. 已完成 ai-test（TT15-TT19）、ai-builder（B15-B18）、ai-license-mgr（L16-L19）、ai-train（T17-T20）、ai-prod（P41-P44）五个模块的 Web 工作台重构。
2. 已完成共享顶栏导航、全流程步骤条与跨模块对象推进入口，统一了 portal 式流程感与模块间跳转体验。
3. 当前开始执行 R16：先收敛 shared / frontend-common / 公共后端底座，再按 ai-train、ai-test、ai-license-mgr、ai-builder、ai-prod、ai-sdk 的顺序推进服务分层与执行模式重构。
4. 当前已完成本轮文档基线刷新：总体架构、整改清单、总体计划已补入平台公共底座、共享 contract validator、execution_mode、事务边界与控制面状态模型等新一轮整改目标。
5. 当前已完成公共后端底座第一批代码收敛：新增 `apps/shared/python/platform_shared/backend`，统一沉淀 config、frontend、snapshot 三类基础能力，并已接入六个后端模块。
6. 当前已完成 Docker 运行链路适配：六个后端镜像已显式复制 `apps/shared/python` 到镜像，且六个后端模块 `app/__init__.py` 已统一采用向上搜索 `shared/python` 的稳健引导方式，兼容本地与容器路径布局。
7. 当前已完成第一轮语法级验证：针对 shared 公共包及六个后端入口/配置/同步服务执行 `python -m py_compile` 校验通过。
8. 当前已完成 ai-train 的 R16 首模块整改：`execution_mode` 已贯通训练任务、执行计划、结果摘要、API 与前端；训练服务已拆分为任务编排、工作区准备、执行调度与结果服务；shared validator 已新增训练结果摘要校验入口并在 ai-train 主链接入。
9. 当前已完成 ai-test 的 R16 第二模块整改：`execution_mode` / `execution_risk` 已贯通测试任务、测试报告、证据链、API 与前端；`test_service.py` 已拆分出 `test_execution_service.py`，测试报告摘要已接入 shared validator，单测 / 批测主链路事务边界已收敛。

### 7.3 未完成

- 1. `shared` 运行时 contract validator 已覆盖训练结果摘要、测试报告摘要、license_tool bundle、SDK 目标包目录、delivery_package（验收清单/版本清单/交付摘要）以及 manifest_model/build/sdk，并已在 ai-train/ai-builder/ai-sdk/ai-prod 接入；下一步仍需覆盖更多 manifest 类 contract（如更多 runtime/plugin 扩展字段与目录级强一致性校验）。
- 2. ai-train、ai-test、ai-license-mgr、ai-builder、ai-prod、ai-sdk 已完成模块级实施。
- 3. 五个前端工作台已抽取共享 HTTP client 并完成接入，且已完成统一工作台壳层组件（`WorkspaceShell`）抽取并在五个前端工作台全部接入，消除了 topbar / workflow strip 重复代码；下一步仍需继续抽象公共 hooks / 状态组件与风险提示层，进一步拆分 `App.tsx` 巨石化。

### 7.4 阶段小结

平台上一轮逻辑自洽整改与 Web 工作台产品化收口已经完成，当前进入 R16。R16 的目标不再是继续堆叠单点功能，而是把已经形成的多模块平台收敛为更稳定的工业级实现：先统一公共后端底座、共享契约执行层与前端公共工作台层，再按模块逐步拆分巨型服务文件，显式区分真实执行与模板 / 仿真路径，收敛事务边界、状态机与控制面状态模型。当前已经完成公共后端底座第一批真实代码落地，并依次在 ai-train、ai-test、ai-license-mgr、ai-builder、ai-prod、ai-sdk 上完成模块级落地样板：platform_shared 已承接训练结果摘要、测试报告摘要、license_tool bundle 与 SDK 目标包目录的共享 contract validator，六个模块已完成核心服务分层整改；ai-prod 已开始推进 RP-04，把 Python 控制面的散落全局运行态收敛为显式状态存储抽象；下一步将继续扩展 shared validator 的覆盖范围。
平台上一轮逻辑自洽整改与 Web 工作台产品化收口已经完成，当前进入 R16。R16 的目标不再是继续堆叠单点功能，而是把已经形成的多模块平台收敛为更稳定的工业级实现：先统一公共后端底座、共享契约执行层与前端公共工作台层，再按模块逐步拆分巨型服务文件，显式区分真实执行与模板 / 仿真路径，收敛事务边界、状态机与控制面状态模型。当前已经完成公共后端底座第一批真实代码落地，并依次在 ai-train、ai-test、ai-license-mgr、ai-builder、ai-prod、ai-sdk 上完成模块级落地样板：platform_shared 已承接训练结果摘要、测试报告摘要、license_tool bundle 与 SDK 目标包目录的共享 contract validator，六个模块已完成核心服务分层整改；ai-prod RP-04 已完成，Python 控制面的散落全局运行态已收敛为显式状态存储抽象；同时 RC-03 已开始落地，`frontend-common` 已新增统一工作台壳层组件（`WorkspaceShell`）并在 ai-builder 接入，作为后续持续抽象公共 hooks / 布局 / 状态组件与风险提示层、消除 `App.tsx` 巨石化的第一步；下一步将继续扩展 shared validator 与前端公共层的覆盖范围。
